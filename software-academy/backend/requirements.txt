# --- Каркас и сервер ---
fastapi>=0.110          # веб-фреймворк (REST API + Swagger автоматически)
uvicorn[standard]>=0.27 # ASGI-сервер, запускает приложение

# --- База данных ---
sqlalchemy>=2.0         # ORM: работа с таблицами как с Python-объектами
psycopg[binary]>=3.1    # драйвер PostgreSQL для Python
alembic>=1.13           # миграции (версии схемы БД)

# --- Настройки ---
pydantic-settings>=2.1  # чтение настроек из .env

# Зависимости авторизации (PyJWT, passlib) добавим на Шаге 4.
# Точные версии можно зафиксировать позже командой: pip freeze > requirements.txt
